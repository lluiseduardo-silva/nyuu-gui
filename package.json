{
  "name": "nyuu-gui",
  "version": "0.1.5",
  "private": true,
  "description": "Web UI wrapper para backups na Usenet via nyuu (fila persistente, editor de config e integração com indexador Curupira).",
  "type": "module",
  "scripts": {
    "install:all": "npm --prefix server install && npm --prefix web install",
    "dev": "node scripts/dev.mjs",
    "build": "npm --prefix web run build",
    "start": "npm --prefix server start"
  }
}
